# pnpm's default symlinked node_modules is unreliable on OneDrive-synced
# Windows folders (symlinks fail to materialize -> "vitest is not recognized").
# The hoisted linker copies files instead of symlinking.
node-linker=hoisted
